server:
  port: 2008

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/23Vvrangler/Examen-Parcial-De-DAD.git #Reposritorio Github
          searchPaths: config-data 
          default-label: main #Rama deL repositorio

  security:
    user:
      name: root
      password: 123456
